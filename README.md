## Building

#### Debian
```bash
$ sudo apt install pkg-config libharfbuzz-dev libfreetype-dev libicu-dev
```

Then install dependecies of raylib for your system from https://github.com/raysan5/raylib/wiki/Working-on-GNU-Linux.
<br>
Then run with:

```bash
$ zig build run
```
